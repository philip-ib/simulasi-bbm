{
  "name": "simulasi-bbm",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && node --watch index.js",
    "dev:client": "cd client && npx vite --port 5173",
    "start": "cd client && npx vite build && cd ../server && node index.js",
    "test": "cd server && NODE_OPTIONS=\"--experimental-vm-modules\" npx jest --forceExit"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
